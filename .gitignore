node_modules/
dist/
.env
*.log

# Runtime artifacts — regenerated by `argus run`. Kept out of git so the repo
# stays clean, but the directories themselves survive via .gitkeep.
data/runs/
data/test-cases.json
data/bugs.json
data/inventory.json
data/triage-log.json
data/triage.log
dashboard/data/
generated-tests/*.spec.ts
playwright-report/
test-results/
data/pw-artifacts/
